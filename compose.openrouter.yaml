services:
  web:
    environment:
      LLM_MODE: openrouter
      OPENROUTER_API_KEY_FILE: /run/secrets/openrouter_api_key
    secrets:
      - openrouter_api_key

secrets:
  openrouter_api_key:
    file: ./secrets/openrouter.key
